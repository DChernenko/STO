@model  STO.WebUI.Models.AddCar

@{
    ViewBag.Title = "Home Page";
}
@using (Html.BeginForm("AddData", "Home", FormMethod.Post, new { @class = "form-horizontal" })){   
        <div class="control-group">
            <label class="control-label" for="">Тип Автомобиля</label>
            <div class="controls">
                @*<input type="text" id="inputEmail" placeholder="Email">*@
                @Html.DropDownList("TypeCar", @ViewBag.ListType as SelectList)
            </div>
        </div>
}

@section scripts{
    @Scripts.Render("~/scripts/Custom/TypeCar.js")
}

